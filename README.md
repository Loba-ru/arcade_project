# arcade_project "Monster Chase"

Платформер на Python с использованием библиотеки Arcade.

## Текущий статус

✅ Коммит №8 - **Переработка и оптимизация архитектуры игры** - 23.05.2026

- Реализована загрузка нескольких уровней через тайловые карты `.tmx`
- Полностью переработана архитектура проекта
- `GameplayView` теперь только создаёт движок игры
- `MyGame` управляет экранами игры и хранит глобальное состояние (монеты, жизни, сложность)
- Добавлен файл `levels.py`, куда перенесены управление персонажем, отрисовка, работа камеры
- Устранены лаги интерфейса: отрисовка текста переведена на `Batch`
- Восстановлено/улучшено управление, перехват клавиш на разных уровнях приложения
- Улучшена читаемость кода: `manager` → `state_manager`, `game` → `game_manager`
- Выполнены многочисленные другие мелкие изменения

## Предыдущие изменения

✅ Коммит №7 - **Интеграция игрового движка и изменение архитектуры** - 23.05.2026

- `GameplayView` теперь использует или настоящую игру, или заглушку (флаг `GAMEPLAY_USE_DUMMY`)
- Добавлены callback-функции для победы, поражения и вызова меню
- Главное окно центрируется на экране (метод `center_window` в `main.py`)
- Исправлена позиция игрока в зависимости от размера тайла
- Добавлено отображение сложности в `GameplayView` и `ResultView`
- Исправлено переключение сложности в `MenuView` (по стрелкам, с циклической логикой)
- Сделаны косметические и другие мелкие изменения

✅ Коммит №6 - **Подготовка к интеграции игрового движка** - 23.05.2026

- Класс `MyGame` больше не наследует `arcade.Window` (готов к внедрению в `GameWindow`)
- `StateManager` управляет `StartView -> MenuView -> GameplayView -> ResultView`
- Выбор сложности мышью/клавишами, сохранение сложности при паузе
- Пауза: ESC в игре -> `MenuView` с надписью "ПАУЗА", ESC в меню -> возврат в игру
- В меню кнопка "НОВАЯ ИГРА", все тексты через `Batch`
- `GameplayView`— пока что как заглушка (W — победа, L — поражение, ESC/M — пауза)

✅ Коммит №5 - **Доработка паттерна State (с заглушками)** - 23.05.2026

- `StateManager` управляет `StartView → GameplayView → MenuView`
- `GameplayView` временная заглушка (без игровой логики)
- Переключение по ESC работает корректно
- Код готов к интеграции настоящей игры

✅ Коммит №4 - **Реализация паттерна State для игровых состояний** - 23.05.2026

- Добавлен `StateManager` и абстрактный `GameState`
- `StartView` и `GameplayView` (заглушки)
- Переключение по пробел и ESC
- `main.py` переписан под управление состояниями

✅ Коммит №3 - **Выделение игрового движка в отдельный модуль** - 23.05.2026

- Класс `MyGame` перенесён в `game_engine.py`
- `main.py` содержит только точку входа
- Логика игры отделена от запуска

✅ Коммит №2 - **Рефакторинг игры, добавление классов существ и констант** - 23.05.2026

- Реализована иерархия существ в `classes.py` (Entity -> Player/Enemy)
- Разрешение окна: 1024×768 (4:3), окно центрируется при запуске
- Все константы вынесены в `constants.py` (добавлены для 16:9 и 16:10)
- Земля прижата к низу, исправлена отрисовка сердечек

✅ Коммит №1 - **Основа платформера с игроком, землёй, камерой и интерфейсом** - 20.05.2026

- Игрок (девочка-авантюристка) может ходить влево/вправо и прыгать
- Физика (гравитация, платформы)
- Земля прижата к низу окна
- Интерфейс: сердечки (жизни), счёт монет (заглушка)
- Камера: два слоя (мир и GUI)
- Мёртвая зона камеры и плавное следование за игроком

